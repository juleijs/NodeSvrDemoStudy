const CommonController = require('./../../common/common_controller')
const Op = require('sequelize').Op
cosnt configWeb3 = require('./../../../config').web3
const exchangeUtils = require('./../../utils/exchange_utils')
const config = require('./../../../config/index')

class WalletController extends CommonController {

  async _init_ (ctx) {

  }
}